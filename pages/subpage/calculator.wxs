var payType =['interest', 'principal'];
var loanTypes = ['providentFund', 'commercial', 'combination'];
var years = [];
for (i = 0; i < 6; i++) {
  years.push((i + 1) * 5);
}

module.exports = {
  loanTypes: loanTypes,
  years: years,
  payType: payType
}